import { ArticoloPagination } from './articolo-pagination';

describe('ArticoloPagination', () => {
  it('should create an instance', () => {
    expect(new ArticoloPagination()).toBeTruthy();
  });
});
