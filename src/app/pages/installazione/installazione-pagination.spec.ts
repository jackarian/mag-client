import { InstallazionePagination } from './installazione-pagination';

describe('InstallazionePagination', () => {
  it('should create an instance', () => {
    expect(new InstallazionePagination()).toBeTruthy();
  });
});
