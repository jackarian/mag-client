import { PermessiSearchPipe } from './permessi-search.pipe';

describe('PermessiSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PermessiSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
